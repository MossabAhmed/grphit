# مراجعة مشروع grphit — لوحة إدارة مصنع الجرافيت

**نطاق المراجعة:** مراجعة المستودع `MossabAhmed/grphit` عند commit `77ff8aa`، بما يشمل `index.html` و`app.js` و`styles.css` و`README.md`، مع تشغيل الواجهة محليًا وفحص منطق الحسابات ومقارنة المتطلبات مع ممارسات أنظمة ERP التصنيعية.

## الخلاصة التنفيذية

المشروع يمثل **نموذج واجهة أمامية أوليًا جيدًا** من حيث تنظيم الأقسام واللغة العربية والتصميم العام. الواجهة تعمل محليًا، وتوفر CRUD أساسيًا للسجلات، والبحث، والاستيراد والتصدير إلى JSON، والطباعة لبعض السجلات.

لكن المشروع **غير جاهز للاستخدام المحاسبي أو التشغيلي الفعلي**. السبب الرئيسي ليس نقص عناصر الواجهة، بل أن البيانات لا تتحرك عبر دورة أعمال مترابطة. الواردة لا تزيد المخزون، والصادرة لا تنقصه، ولا توجد تكلفة وحدة أو تكلفة بضاعة مباعة أو دفاتر قبض ودفع أو قيود محاسبية. لذلك فإن أرقام الربح والمخزون والتقارير قد تبدو صحيحة بصريًا، لكنها لا تمثل الوضع الحقيقي للمصنع.

أوصي بإيقاف الاعتماد على أرقام لوحة التحكم الحالية إلى أن يتم بناء **دفتر حركات موحد** للمخزون و**نموذج محاسبي واضح**. يمكن الإبقاء على الواجهة الحالية كنموذج UX، لكن يجب إعادة بناء طبقة البيانات والمنطق قبل إضافة مزيد من الشاشات.

## نتيجة الفحص السريع

| المجال | الحالة الحالية | التقييم |
|---|---|---:|
| تحميل الواجهة والتنقل | تعمل الأقسام والنماذج محليًا | جيد كنموذج أولي |
| CRUD والبحث | إضافة وتعديل وحذف وبحث نصي | مقبول مع قيود مهمة |
| حفظ البيانات | `localStorage` في متصفح واحد | غير مناسب للإنتاج |
| ترابط الواردات بالمخزون | غير موجود | حرج |
| ترابط الصادرات بالمخزون | غير موجود | حرج |
| تكلفة المخزون وCOGS | غير موجودة | حرج |
| الربح والتقارير الزمنية | حسابات مختلطة وغير مكتملة | حرج |
| الإنتاج والتصنيع | لا توجد أوامر إنتاج أو BOM | حرج لمصنع |
| الصادرات التجارية | سجل مبيعات بسيط فقط | غير كافٍ |
| التدقيق والصلاحيات | غير موجود | حرج |
| الأمان والتحقق | XSS محتمل وتحقق ضعيف | عالٍ |

## نتائج الاختبارات

تم تشغيل التطبيق عبر خادم محلي وفحصه بمتصفح فعلي. ظهرت لوحة التحكم والأقسام والنماذج دون خطأ تحميل ظاهر. كما تم تشغيل `node --check app.js` بنجاح بعد تصحيح أمر الفحص الأولي الذي كان يحتوي على خطأ في أمر shell وليس في JavaScript.

اختبار منطقي مستقل أكد الانفصال بين الجداول. عند وجود رصيد افتتاحي قدره 10 وحدات، وواردة قدرها 20، وصادرة قدرها 8، بقيت كمية المخزون 10 لأن الكود لا ينفذ أي تحديث للمخزون. الكمية المتوقعة في نظام مترابط هي 22 وحدة.

تظهر البيانات التجريبية كذلك إجمالي إيرادات قدره 32,700 د.ل.، ومصاريف قدرها 30,900 د.ل.، وربح قدره 1,800 د.ل.، بينما قيمة الواردات 29,800 د.ل. لا تدخل في معادلة الربح. هذا ليس بالضرورة خطأ محاسبيًا في كل نموذج، لكنه خطأ تصميمي هنا لأن النظام لا يملك أصلًا سياسة دورية أو مستمرة لتقييم المخزون ولا COGS.

## المشكلات الحرجة في المخزون

### 1. لا يوجد دفتر حركات مخزون

في `app.js:227-239` تتم إضافة سجل واردة إلى `state.imports` فقط. وفي `app.js:242-255` تتم إضافة سجل صادرة إلى `state.exports` فقط. لا يوجد إنشاء لحركة `receipt` أو `issue` ولا تعديل لـ `state.inventory`.

النتيجة أن كمية المخزون المعروضة ليست ناتجًا عن الواردات والصادرات. وهي قابلة للتعديل يدويًا دون معرفة السبب أو المستخدم أو التاريخ.

**الإصلاح المطلوب:** إنشاء دفتر حركات غير قابل للتلاعب المباشر، مثل:

```js
{
  id, type: 'receipt' | 'issue' | 'adjustment' | 'production-consumption' | 'production-output',
  itemId,
  qty,
  unitCost,
  warehouseId,
  lotId,
  referenceType,
  referenceId,
  date,
  createdBy,
  createdAt
}
```

ويُحسب الرصيد من الحركات أو يُحدّث عبر خدمة مركزية واحدة، وليس عبر كل نموذج على حدة.

### 2. الصنف يُكتب كنص بدل معرف صنف

الواردة والصادرات تستخدمان `item` كنص حر. لذلك يمكن تسجيل `مسحوق جرافيت` و`مسحوق جرافيت ` أو اسم مختلف قليلًا، ولا يستطيع النظام الربط بثبات مع صنف المخزون أو وحدته أو تكلفته.

**الإصلاح المطلوب:** جدول أصناف مستقل يحوي `sku`, `name`, `category`, `unit`, `trackLots`, `reorderPoint`, `standardCost`، مع قوائم اختيار مرتبطة بـ `itemId` بدل النص الحر.

### 3. لا توجد مواقع تخزين أو كميات محجوزة أو متوقعة

المخزون الحالي يعرض كمية واحدة فقط. لا يوجد مستودع أو موقع، ولا كمية متاحة للبيع، ولا محجوزة، ولا واردة بالطريق، ولا صادرة متوقعة. توثيق Odoo يميز بين On Hand وFree to Use وIncoming وOutgoing، ويعرض تاريخ الحركات والتوريد وإعادة الطلب والتوقعات [1] [2].

**الإصلاح المطلوب:** دعم المستودعات والمواقع، والحقول `onHand`, `reserved`, `incoming`, `outgoing`, `available`، مع تقرير حركة صنف وتوقع الرصيد.

### 4. لا توجد قيمة مخزون أو طريقة تقييم

النظام يسجل تكلفة إجمالية للواردة، لكنه لا يسجل تكلفة الوحدة، ولا يحدد هل التقييم Standard Cost أو Average Cost أو FIFO. توثيق Odoo يوضح أن طرق التكلفة تغير قيمة المخزون وتكلفة الصادر، وأن Average Cost وFIFO ينتجان قيمًا مختلفة عند اختلاف أسعار الشراء [2].

**الإصلاح المطلوب:** اختيار سياسة تقييم واحدة على مستوى المصنع، مع تنفيذها واختبارها قبل اعتماد الربح. الخيار العملي الأول هو المتوسط المرجح:

`متوسط التكلفة الجديد = (قيمة الرصيد القديم + قيمة الواردة) ÷ (كمية الرصيد القديم + كمية الواردة)`

وعند الصادر:

`COGS = الكمية المصروفة × تكلفة الوحدة المعتمدة وقت الصرف`

### 5. الواردات لا تفصل بين أمر شراء والاستلام والفاتورة

الواردة الحالية سجل واحد يحتوي المورد والصنف والكمية والتكلفة والتاريخ. لا يوجد أمر شراء، استلام جزئي، فاتورة مورد، حالة دفع، عملة المورد، أو فرق بين الكمية المطلوبة والمستلمة والمفوترة.

**الإصلاح المطلوب:** دورة `Purchase Order → Receipt → Vendor Bill → Payment`، مع حالات واضحة ومنع اعتبار الطلب مستلمًا أو مدفوعًا دون إجراء صريح.

### 6. التكلفة الشاملة للاستيراد غير موجودة

النص في الواجهة يذكر الشحن والجمارك، لكن نموذج الواردة لا يحتوي أيًا منهما. لا توجد مصاريف نقل أو تأمين أو رسوم ميناء أو جمارك أو تخليص ولا طريقة توزيعها على الأصناف.

تكلفة الوصول تشمل عادة سعر المنتج والشحن والرسوم والضرائب والتأمين والمناولة والتخليص [3]. كما أن توزيع التكلفة قد يكون بالقيمة أو الكمية أو الوزن أو الحجم، ويؤثر مباشرة على تكلفة الوحدة والربحية [3].

**الإصلاح المطلوب:** إضافة سجل `landedCost` مرتبط بالشحنة، وبنود تكلفة قابلة للتوزيع، وطريقة allocation، ثم ترحيل الناتج إلى قيمة المخزون أو مصروف مناسب وفق السياسة المحاسبية.

## المشكلات الحرجة في الصادرات والإيرادات

### 1. الصادرة لا تخصم المخزون ولا تمنع الرصيد السالب

في `app.js:242-255` يتم حفظ الصادرة فقط. يستطيع المستخدم تصدير كمية أكبر من المتاح، ولا يظهر تحذير ولا رفض ولا حجز.

**الإصلاح المطلوب:** عند إنشاء أمر بيع، احجز الكمية. عند التسليم، نفذ حركة issue بعد التحقق من `availableQty`. يجب أن تكون الكمية الصادرة مرتبطة بـ `itemId` ومستودع ودفعة، لا باسم نصي فقط.

### 2. الإيراد ليس فاتورة مكتملة

حقل `revenue` قيمة إجمالية مبهمة. لا توجد عملة، سعر وحدة، خصم، ضريبة، تكلفة شحن للعميل، شروط دفع، تاريخ استحقاق، تحصيل، دفعة جزئية، أو رصيد مستحق.

**الإصلاح المطلوب:** فصل:

- أمر البيع.
- التسليم.
- فاتورة العميل.
- المقبوضات.
- الرصيد المستحق.

وحساب `الإيراد الصافي` و`الضريبة` و`الذمم المدينة` بصورة مستقلة.

### 3. لا توجد تكلفة بضاعة مباعة أو هامش ربح

المعادلة الحالية في `app.js:370-374` هي:

`revenue - expenses - payroll`

وهذه ليست صافي ربح صناعيًا لأنها لا تتضمن COGS، ولا التغير في المخزون، ولا تكلفة الإنتاج، ولا تكاليف الوصول، ولا إهلاك المعدات، ولا مصروفات مرتبطة بالبيع. في نموذج الجرد المستمر، تُسجل تكلفة البضاعة عند بيعها؛ وفي الجرد الدوري تُعالج قيمة المخزون والتغير في المخزون عند الإقفال [2]. يجب اختيار سياسة واحدة لا خلطهما.

### 4. الصادرات التجارية غير ممثلة

قسم الصادرات هو في الواقع قسم مبيعات داخلي بسيط. لا توجد دولة ومدينة وميناء وشروط Incoterms ووسيلة نقل ورقم حاوية ووزن وحجم ومستندات وشهادة منشأ وحالة جمركية وتكلفة تصدير.

**الإصلاح المطلوب:** إضافة Shipment/Export Order منفصل عن الفاتورة، مع بيانات العميل والبلد والعملة والشحن والتخليص والمستندات وحالة الشحنة.

## المشكلات في المصروفات والرواتب

### 1. المصروفات غير مصنفة محاسبيًا

النموذج يحتوي `type`, `description`, `amount`, `date` فقط. لا توجد حسابات، مركز تكلفة، مورد، وسيلة دفع، ضريبة، مرفق، أو حالة اعتماد.

**الإصلاح المطلوب:** إضافة `accountId`, `costCenterId`, `vendorId`, `paymentAccountId`, `taxAmount`, `attachment`, `status`, `approvedBy`، مع منع حذف المصروف المرحّل واستبداله بعكس قيد أو إلغاء موثق.

### 2. الرواتب تدخل في الربح دون فترة زمنية

العمال لا يملكون شهرًا أو تاريخ استحقاق. في `app.js:373` و`app.js:657` يتم جمع راتب كل عامل في كل مرة. وفي `app.js:694` يُضاف مجموع رواتب جميع العمال إلى أي شهر مختار.

بالتالي، تقرير سبتمبر سيضم رواتب العمال حتى لو لم تُسجل رواتب سبتمبر، وتقرير أكتوبر سيضمها مرة ثانية.

**الإصلاح المطلوب:** إنشاء Payroll Period وسجل راتب شهري فعلي لكل عامل، مع الحضور والغياب والسلف والاستقطاعات والإضافات وحالة الدفع. يجب أن يرتبط كل قيد راتب بشهر واحد وتاريخ دفع وحساب مصروف.

### 3. السلفة تُخصم من الصافي لكنها ليست معاملة مالية

الكود يحسب `salary - advance`، لكنه لا ينشئ حركة صرف للسلفة ولا رصيدًا مستحقًا على العامل ولا يحدد هل السلفة تخص الشهر الحالي.

## مشكلات التقارير والمؤشرات

### 1. اختيار الشهر لا يطبق على جميع المؤشرات

`renderReports()` يحسب الإيرادات والمصاريف والرواتب والواردات من كامل المصفوفات في `app.js:653-659`، رغم وجود `selectedMonth`. التصفية الشهرية تطبق فقط في `renderMonthlySummary()` على الواردات والصادرات والمصاريف، بينما الرواتب لا تُصفى إطلاقًا.

**الإصلاح المطلوب:** دالة موحدة `filterByPeriod(records, from, to)` تستخدمها كل مؤشرات التقرير، مع خيار فترة مخصصة وليس شهرًا فقط.

### 2. المصاريف والواردات والصادرات لا تملك حالات محاسبية

التقرير يجمع السجلات بمجرد وجودها. لا يوجد تمييز بين مسودة ومُعتمد وملغى ومدفوع ومستحق. هذا يجعل التقرير قابلًا للتشويه بحذف أو تعديل سجل بعد صدور التقرير.

### 3. التقرير لا يبين الرصيد الافتتاحي والختامي

لا توجد تسوية مخزون أو رصيد نقدي أو ذمم أو ضريبة مستحقة أو تكلفة مبيعات. لذلك لا يمكن استخدام التقرير للإقفال أو المراجعة.

### 4. الرسم البياني مضلل عند الخسارة

في `app.js:397-399` يستخدم صافي الربح `Math.max(netProfit, 0)`. عند الخسارة تظهر قيمة صفر بدل قيمة سالبة، فيخفي المشكلة بدل إبرازها.

**الإصلاح المطلوب:** عرض الخسارة بقيمة سالبة بلون واضح، وتحديد مقياس مناسب يسمح بمقارنة القيم السالبة والموجبة.

## الأمان وسلامة البيانات

### 1. خطر XSS عبر `innerHTML`

البيانات التي يدخلها المستخدم تُحقن مباشرة في `innerHTML` في الجداول والبطاقات والتنبيهات والتقارير والطباعة، مثل `app.js:385-418` و`app.js:476-490` و`app.js:573-641`. إدخال قيمة مثل `<img src=x onerror=alert(1)>` قد يؤدي إلى تنفيذ HTML/JavaScript داخل الصفحة أو نافذة الطباعة.

**الإصلاح المطلوب:** استخدام `textContent` وعناصر DOM، أو دالة escaping موحدة قبل أي إدراج في HTML. لا يكفي الاعتماد على أن التطبيق محلي، لأن JSON المستورد قد يأتي من ملف خارجي.

### 2. localStorage ليس قاعدة بيانات أو نسخًا احتياطيًا آمنًا

البيانات محصورة في متصفح وجهاز واحد. حذف بيانات الموقع أو تغيير المتصفح قد يفقدها، ولا توجد مستخدمون أو صلاحيات أو سجل تغييرات أو قفل فترة مالية أو تعارض بين جهازين.

**الإصلاح المطلوب:** لنسخة تجريبية يمكن الإبقاء على localStorage مع versioning وschema migration ونسخ احتياطية دورية. للاستخدام الفعلي يجب نقل البيانات إلى API وقاعدة بيانات مع مصادقة وصلاحيات وتدقيق ونسخ احتياطي.

### 3. الاستيراد لا يتحقق من مخطط البيانات بما يكفي

`mergeState()` يتحقق من أن بعض الحقول مصفوفات، لكنه لا يتحقق من أنواع الحقول أو القيم السالبة أو المعرفات المكررة أو التواريخ غير الصحيحة. كما يمكن استيراد سجلات غير صالحة فتظهر `NaN` أو قيم غير منطقية.

**الإصلاح المطلوب:** schema validation، version field، migration، فحص حدود الأرقام، منع `NaN`، وتقرير أخطاء يحدد السجل والحقل.

### 4. العملة مفروضة رغم وجود إعداد لها

في `app.js:54` و`app.js:303-304` و`app.js:350-351` يتم فرض `LYD`، وحقل العملة يصبح للعرض فقط في `app.js:360-365`. هذا يخلق تناقضًا بين واجهة إعدادات تدعي قابلية تغيير العملة ومنطق يمنعها.

**الإصلاح المطلوب:** إما إزالة إعداد العملة وشرح أن النظام خاص بالدينار الليبي، أو دعم العملة فعليًا مع عملة المستند وسعر الصرف ومصدره.

## نواقص التصنيع الخاصة بمصنع الجرافيت

المشروع يعرض نفسه كنظام لمصنع لكنه لا يحتوي على دورة تصنيع. لا توجد:

- قائمة مواد BOM تربط الخام بالمنتج النهائي.
- أمر إنتاج وحالة الأمر.
- استهلاك خام فعلي وناتج إنتاج فعلي.
- فاقد وخردة ومنتجات جانبية.
- مراحل تشغيل ومراكز عمل وزمن إنتاج.
- تكلفة مباشرة للعمل والطاقة والصيانة.
- جودة وفحص ورفض.
- أرقام تشغيلات Lots أو أرقام تسلسلية.
- تتبع من المورد إلى المنتج النهائي ومن المنتج إلى العملاء.

أنظمة التصنيع الناضجة تربط BOM وأوامر العمل واستهلاك المواد والتكاليف والتقارير، وتوفر تقارير تأخير وتحليل إنتاج وكفاءة المعدات [4]. كما تستخدم أرقام التشغيلات أو الأرقام التسلسلية لربط المنتج النهائي بالمواد الداخلة فيه، وهو أمر مهم للاستدعاء والتحقيق في الجودة [5].

## مشكلات واجهة وتجربة الاستخدام

التصميم العام مرتب ومناسب للغة العربية، لكن توجد نقاط تحسين عملية:

| الملاحظة | الأثر | التحسين |
|---|---|---|
| الجداول تعرض جميع السجلات في صفحة واحدة | ضعف الأداء عند نمو البيانات | pagination وvirtualization |
| لا توجد حالات تحميل أو أخطاء أو نجاح موحدة | المستخدم لا يعرف نتيجة العملية | toast ونموذج أخطاء واضح |
| الحذف النهائي متاح لكل سجل | خطر فقد البيانات | archive/void وسجل تدقيق |
| لا توجد حقول إلزامية في التعديل مثل `min` و`step` | إدخال قيم سالبة أو صفرية غير مناسبة | نفس قواعد التحقق في الإضافة والتعديل |
| لا توجد اختصارات أو فلاتر حالة/تاريخ | صعوبة التشغيل اليومي | فلاتر مركبة وتصدير تقارير |
| العملة والتواريخ لا تعرض دائمًا بصيغة موحدة | لبس في المستندات | formatter مركزي للمال والتاريخ والمنطقة الزمنية |
| زر إعادة البيانات التجريبية قريب من إجراءات البيانات | خطر تشغيله بالخطأ | وضعه في إعدادات منفصلة وطلب كتابة كلمة تأكيد |

## خطة تنفيذ مقترحة حسب الأولوية

### المرحلة الأولى: منع الأرقام المضللة

1. إيقاف حساب الربح الحالي أو تسميته بوضوح `تدفق نقدي مبسط`.
2. تعريف نموذج الأصناف بالمعرفات والوحدات.
3. بناء دفتر حركات مخزون مركزي.
4. ربط الواردات بالمخزون وربط الصادرات بالتسليم والتحقق من الكمية.
5. إضافة اختبار آلي لكل سيناريو: استلام، تسليم، تعديل، إلغاء، كمية غير كافية.
6. إصلاح التقرير الشهري بحيث يرشح كل أنواع السجلات بنفس الفترة.

### المرحلة الثانية: التقييم والمحاسبة

1. اختيار Average Cost أو FIFO أو Standard Cost وتوثيقه.
2. حساب COGS عند التسليم.
3. إضافة landed cost للاستيراد.
4. إضافة دليل حسابات ومراكز تكلفة وذمم ومقبوضات ومدفوعات.
5. إضافة فترات رواتب شهرية بدل جمع رواتب العمال مباشرة.
6. إضافة حالات ومسار اعتماد وقفل الفترات.

### المرحلة الثالثة: التصنيع والتصدير

1. BOM وأوامر إنتاج واستهلاك خام وناتج نهائي.
2. الفاقد والمنتجات الجانبية وتكلفة التشغيل.
3. الدفعات والتتبع والجودة.
4. أوامر التصدير والشحن والمستندات والتكاليف المرتبطة.
5. تقارير هامش الربح حسب المنتج والعميل والبلد والشحنة.

### المرحلة الرابعة: الإنتاج والحوكمة

1. نقل التخزين من localStorage إلى قاعدة بيانات.
2. تسجيل دخول وصلاحيات حسب الدور.
3. audit log لكل إنشاء وتعديل وإلغاء.
4. نسخ احتياطي واسترجاع واختبارات ترحيل schema.
5. اختبارات end-to-end للعمليات المالية والمخزنية.

## نموذج بيانات مقترح مختصر

| الكيان | أهم الحقول |
|---|---|
| Product | `id, sku, name, category, unit, tracking, reorderPoint` |
| Warehouse | `id, name, locations` |
| StockMovement | `id, productId, type, qty, unitCost, warehouseId, lotId, referenceId, date` |
| PurchaseOrder | `id, supplierId, lines, currency, status, expectedDate` |
| Receipt | `id, purchaseOrderId, receivedLines, warehouseId, date` |
| LandedCost | `id, receiptId, freight, duty, insurance, handling, allocationMethod` |
| SalesOrder | `id, customerId, lines, currency, status, dueDate` |
| Delivery | `id, salesOrderId, issuedLines, warehouseId, date` |
| Invoice | `id, partnerId, lines, tax, total, status, dueDate` |
| Payment | `id, invoiceId, accountId, amount, date, method` |
| BOM | `id, productId, version, components, yield` |
| ManufacturingOrder | `id, bomId, plannedQty, consumed, produced, scrap, status` |
| PayrollPeriod | `id, month, lines, gross, advances, deductions, net, status` |
| AuditLog | `id, entity, entityId, action, before, after, userId, createdAt` |

## الحد الأدنى لاختبارات القبول قبل اعتبار النظام صالحًا

| السيناريو | النتيجة المتوقعة |
|---|---|
| استلام 20 وحدة بتكلفة 200 | زيادة الرصيد 20 وزيادة قيمة المخزون 200 |
| بيع 8 وحدات من الرصيد المتاح | نقص الرصيد 8 وإنشاء COGS حسب طريقة التقييم |
| بيع كمية أكبر من المتاح | رفض العملية أو إنشاء backorder، وعدم السماح برصيد سالب دون صلاحية |
| إلغاء استلام | عكس حركة الاستلام مع سجل تدقيق، لا حذف صامت |
| واردة بتكلفة شحن وجمارك | توزيع landed cost وتحديث تكلفة الوحدة |
| راتب سبتمبر | يظهر في سبتمبر فقط، ولا يتكرر في أكتوبر |
| فاتورة غير مدفوعة | تظهر في الذمم، لا كتحصيل نقدي |
| خسارة شهرية | تعرض كقيمة سالبة لا كصفر |
| استيراد JSON تالف | رفض الملف مع تحديد الحقول الخاطئة دون إتلاف الحالة الحالية |
| إدخال HTML في اسم صنف | يعرض كنص آمن ولا ينفذ ككود |

## الحكم النهائي

**المشروع مناسب كنقطة بداية لواجهة dashboard محلية، وغير مناسب حاليًا كنظام تشغيل لمصنع أو مصدر رسمي للإيرادات والمخزون والتقارير.** أهم خطوة ليست إضافة رسوم بيانية أو صفحات جديدة، بل بناء مصدر حقيقة واحد للحركات المخزنية والمالية، ثم جعل كل واجهة وتقرير يقرأ منه.

إذا تم تنفيذ المرحلة الأولى، يمكن تحويل المشروع إلى نموذج عملي قابل للتوسع. أما توسيع الواجهة الحالية مع بقاء الجداول منفصلة فسيزيد كمية البيانات غير المترابطة ويجعل اكتشاف الأخطاء وتصحيحها أصعب.

## المراجع

[1]: https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/inventory/warehouses_storage/reporting/stock.html "Odoo 19 Stock Report Documentation"

[2]: https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/inventory/inventory_valuation/cheat_sheet.html "Odoo 19 Inventory Valuation Cheat Sheet"

[3]: https://www.finaleinventory.com/guides/landed-cost/ "Landed Cost: Complete Guide to Calculating Total Import and Product Costs"

[4]: https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/manufacturing.html "Odoo 19 Manufacturing Documentation"

[5]: https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/manufacturing/workflows/manufacture_lots_serials.html "Odoo 19 Manufacture with Lots and Serial Numbers"

**إعداد التقرير:** Manus AI
